{
  "name": "server-choromando",
  "version": "1.0.0",
  "description": "Rest Api Choromando",
  "main": "choromandoServer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/choromandoServer.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "firebase-admin": "^5.10.0",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "express": "^4.17.3",
    "fs": "0.0.1-security",
    "https": "^1.0.0",
    "rimraf": "^2.6.2"
  }
}
